import Image from "next/image";
import React from "react";

export type MembersProps = {
    title: string, 
    imageUrl: string, 
}

const Members = ({title,imageUrl}: MembersProps)=> {
  return (
      <div>
        
      </div>
  );
};

export default Members;
